#pragma once
// x, y座標の単位ベクトル(WIDTH_INTERVAL, HEIGHT_INTERVAL)
// (x, y)の座標は(WIDTH, HEIGHT)
#define WIDTH_INTERVAL 4
#define WIDTH 15
#define HEIGHT_INTERVAL 2
#define HEIGHT 15
#define DEBUG false
#define CLEARTIME 10
#define SPEED 60
